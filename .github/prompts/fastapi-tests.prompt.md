# Generate Router Unit Tests

You are to write pytest unit tests for the FastAPI router shown in the context
- each router gets its own test file under the `backend/src/tests` directory
- Use fixtures defined in `backend/src/tests/conftest.py`
- Create new fixtures as needed